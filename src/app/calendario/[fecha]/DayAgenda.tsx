"use client";import Link from "next/link";import {useRouter} from "next/navigation";import {FormEvent,useState} from "react";import {TaskCreateModal} from "@/components/TaskCreateModal";
type Tarea={id:string;descripcion:string;turno:"MANANA"|"TARDE";estado:string;reprogramadaAId:string|null;motivoReprogramacion:string|null;pedido:{nombre:string};reprogramadaA:{fecha:string}|null};type Pedido={id:string;nombre:string};const labels:Record<string,string>={PENDIENTE:"Pendiente",EN_PROCESO:"En proceso",REALIZADO:"Realizado",NO_CUMPLIDO:"No cumplido"};
export function DayAgenda({fecha,tareas,pedidos}:{fecha:string;tareas:Tarea[];pedidos:Pedido[]}){const router=useRouter();const [selected,setSelected]=useState<Tarea|null>(null),[reschedule,setReschedule]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState("");const date=new Date(`${fecha}T12:00:00`),title=new Intl.DateTimeFormat("es-AR",{weekday:"long",day:"numeric",month:"long"}).format(date);const adjacent=(n:number)=>{const d=new Date(date);d.setDate(d.getDate()+n);return d.toLocaleDateString("en-CA")};async function status(estado:string){if(!selected)return;setBusy(true);const r=await fetch(`/api/tareas/${selected.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({estado})});if(r.ok){setSelected(null);router.refresh()}else setError("No se pudo actualizar");setBusy(false)}async function reprogramar(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!selected)return;setBusy(true);const r=await fetch(`/api/tareas/${selected.id}/reprogramar`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});if(r.ok){setSelected(null);setReschedule(false);router.refresh()}else{setError((await r.json()).error);setBusy(false)}}const section=(turno:"MANANA"|"TARDE")=><section className="shift"><h3>{turno==="MANANA"?"Mañana":"Tarde"}</h3>{tareas.filter(t=>t.turno===turno).map(t=><article className={`task-card ${t.reprogramadaAId?"rescheduled":""}`} key={t.id} onClick={()=>!t.reprogramadaAId&&setSelected(t)}><i className={`task-dot ${t.estado.toLowerCase()}`}/><div><strong>{t.descripcion}</strong><span>{t.pedido.nombre}</span>{t.reprogramadaA&&<Link onClick={e=>e.stopPropagation()} href={`/calendario/${t.reprogramadaA.fecha.slice(0,10)}`}>Reprogramado <span className="material-symbols-outlined">arrow_forward</span></Link>}</div><em>{labels[t.estado]}</em></article>)}{!tareas.some(t=>t.turno===turno)&&<p className="no-tasks">Sin tareas</p>}</section>;return <><main className="day-main"><div className="day-heading"><h2>{title}</h2><div><Link href={`/calendario/${adjacent(-1)}`}><span className="material-symbols-outlined">chevron_left</span></Link><Link href={`/calendario/${adjacent(1)}`}><span className="material-symbols-outlined">chevron_right</span></Link></div></div>{section("MANANA")}{section("TARDE")}</main><TaskCreateModal pedidos={pedidos} fecha={fecha}/>{selected&&<div className="modal-backdrop">{!reschedule?<div className="task-modal"><header><h2>{selected.descripcion}</h2><button onClick={()=>setSelected(null)}><span className="material-symbols-outlined">close</span></button></header><p><b>Cliente:</b> {selected.pedido.nombre}</p><p><b>Turno:</b> {selected.turno==="MANANA"?"Mañana":"Tarde"}</p><p><b>Estado:</b> {labels[selected.estado]}</p><div className="state-buttons">{Object.entries(labels).map(([v,l])=><button className={v.toLowerCase()} disabled={busy} onClick={()=>status(v)} key={v}>{l}</button>)}</div><button className="reschedule-button" onClick={()=>setReschedule(true)}>Reprogramar</button></div>:<form className="task-modal" onSubmit={reprogramar}><header><h2>Reprogramar tarea</h2><button type="button" onClick={()=>setReschedule(false)}><span className="material-symbols-outlined">arrow_back</span></button></header><label>¿Por qué se reprograma? (opcional)<textarea name="motivo" rows={2}/></label><div className="form-row"><label>Nueva fecha<input name="nuevaFecha" type="date" defaultValue={adjacent(1)} required/></label><label>Nuevo turno<select name="nuevoTurno" defaultValue={selected.turno}><option value="MANANA">Mañana</option><option value="TARDE">Tarde</option></select></label></div><label>Descripción<textarea name="descripcion" defaultValue={selected.descripcion} rows={3} required/></label><p className="form-error">{error}</p><button className="primary-action" disabled={busy}>{busy?"Reprogramando…":"Confirmar reprogramación"}</button></form>}</div>}</>}
