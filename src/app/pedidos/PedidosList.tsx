"use client";import Link from "next/link";import { useState } from "react";
type P={id:string;nombre:string;tipoTrabajo:string;descripcion:string|null;fechaConfirmacion:string;montoTotal:string;estado:"EN_PROCESO"|"ENTREGADO"};
const money=new Intl.NumberFormat("es-AR",{style:"currency",currency:"ARS",maximumFractionDigits:2});const date=new Intl.DateTimeFormat("es-AR",{day:"2-digit",month:"short",year:"numeric",timeZone:"UTC"});
export function PedidosList({pedidos,role}:{pedidos:P[];role:string}){const [q,setQ]=useState("");const shown=pedidos.filter(p=>p.nombre.toLocaleLowerCase("es").includes(q.toLocaleLowerCase("es")));return <><section className="orders-tools"><div className="search"><span className="material-symbols-outlined">search</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar pedidos..."/></div>{role==="ADMIN"&&<Link className="new-order" href="/pedidos/nuevo"><span className="material-symbols-outlined">add</span>Nuevo pedido</Link>}</section><section className="orders-grid">{shown.map(p=><Link href={`/pedidos/${p.id}`} className={`order-card ${p.estado==="ENTREGADO"?"delivered":""}`} key={p.id}><i/><div><header><h2>{p.nombre}</h2><span className={`status ${p.estado.toLowerCase()}`}>{p.estado==="ENTREGADO"?"Entregado":"En proceso"}</span></header><p>{p.descripcion||p.tipoTrabajo}</p><footer><span><span className="material-symbols-outlined">{p.estado==="ENTREGADO"?"check_circle":"calendar_today"}</span>{date.format(new Date(p.fechaConfirmacion))}</span><strong>{money.format(Number(p.montoTotal))}</strong></footer></div></Link>)}</section>{!shown.length&&<p className="empty">No hay pedidos que coincidan con la búsqueda.</p>}</>}
