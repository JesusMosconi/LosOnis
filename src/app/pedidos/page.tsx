import { redirect } from "next/navigation";import { prisma } from "@/lib/prisma";import { getSession } from "@/lib/session";import { AppHeader } from "@/components/AppHeader";import { BottomNav } from "@/components/BottomNav";import { PedidosList } from "./PedidosList";
export const dynamic="force-dynamic";export default async function Page(){if(!await getSession())redirect("/login");const rows=await prisma.pedido.findMany({orderBy:{fechaConfirmacion:"desc"}});const pedidos=rows.map(p=>({...p,fechaConfirmacion:p.fechaConfirmacion.toISOString(),montoTotal:p.montoTotal.toString(),createdAt:p.createdAt.toISOString(),updatedAt:p.updatedAt.toISOString()}));return <><AppHeader/><main className="orders-main"><PedidosList pedidos={pedidos}/></main><BottomNav/></>}
