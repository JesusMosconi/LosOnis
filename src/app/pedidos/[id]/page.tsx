import { notFound,redirect } from "next/navigation";import { prisma } from "@/lib/prisma";import { getSession } from "@/lib/session";import { BackHeader } from "@/components/AppHeader";import { DetailActions } from "./DetailActions";
const money=new Intl.NumberFormat("es-AR",{style:"currency",currency:"ARS"});export default async function Page({params}:{params:Promise<{id:string}>}){if(!await getSession())redirect("/login");const {id}=await params;const p=await prisma.pedido.findUnique({where:{id}});if(!p)notFound();return <><BackHeader title={`Pedido #${p.id.slice(-6).toUpperCase()}`}/><main className="detail-main"><DetailActions id={p.id} estado={p.estado}/><section className="info-card"><div><label>Datos del cliente</label><h2>{p.nombre}</h2><a href={`tel:${p.telefono}`}><span className="material-symbols-outlined">phone</span>{p.telefono}</a></div><hr/><div><label>Tipo de trabajo</label><p>{p.tipoTrabajo}</p>{p.descripcion&&<p>{p.descripcion}</p>}</div><hr/><div className="amount"><label>Monto total</label><strong>{money.format(Number(p.montoTotal))}</strong></div></section><section className="tasks-placeholder"><header><h2>Tareas del pedido</h2><button disabled><span className="material-symbols-outlined">add</span>Agregar tarea</button></header><p>Las tareas se incorporarán en el próximo paso.</p></section></main></>}
