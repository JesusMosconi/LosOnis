import { notFound,redirect } from "next/navigation";import { prisma } from "@/lib/prisma";import { getSession } from "@/lib/session";import { BackHeader } from "@/components/AppHeader";import { PedidoForm } from "@/components/PedidoForm";
export default async function Page({params}:{params:Promise<{id:string}>}){if(!await getSession())redirect("/login");const {id}=await params;const p=await prisma.pedido.findUnique({where:{id}});if(!p)notFound();return <><BackHeader title="Editar pedido" href={`/pedidos/${id}`}/><main className="form-main"><PedidoForm id={id} pedido={{nombre:p.nombre,telefono:p.telefono??"",tipoTrabajo:p.tipoTrabajo,descripcion:p.descripcion??"",fechaConfirmacion:p.fechaConfirmacion.toISOString().slice(0,10),montoTotal:p.montoTotal.toString()}}/></main></>}
