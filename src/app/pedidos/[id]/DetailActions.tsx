"use client";import { useState } from "react";import { useRouter } from "next/navigation";import Link from "next/link";
export function DetailActions({id,estado}:{id:string;estado:string}){const router=useRouter();const [open,setOpen]=useState(false);const [busy,setBusy]=useState(false);async function change(value:string){setBusy(true);await fetch(`/api/pedidos/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({estado:value})});setBusy(false);router.refresh()}async function remove(){setBusy(true);const r=await fetch(`/api/pedidos/${id}`,{method:"DELETE"});if(r.ok){router.push("/pedidos");router.refresh()}else setBusy(false)}return <><section className="status-card"><h2>Estado del pedido</h2><select className={estado==="ENTREGADO"?"is-delivered":""} value={estado} onChange={e=>change(e.target.value)} disabled={busy}><option value="EN_PROCESO">En proceso</option><option value="ENTREGADO">Entregado</option></select></section><div className="detail-actions"><Link href={`/pedidos/${id}/editar`}><span className="material-symbols-outlined">edit</span>Editar</Link><button onClick={()=>setOpen(true)}><span className="material-symbols-outlined">delete</span>Borrar</button></div>{open&&<div className="modal-backdrop" role="presentation"><div className="modal" role="alertdialog" aria-modal="true"><h2>Eliminar pedido</h2><p>¿Seguro que querés eliminar este pedido? Esta acción no se puede deshacer.</p><div><button onClick={()=>setOpen(false)} disabled={busy}>Cancelar</button><button className="danger" onClick={remove} disabled={busy}>{busy?"Eliminando…":"Eliminar"}</button></div></div></div>}</>}
