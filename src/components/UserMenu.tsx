"use client";import {useEffect,useRef,useState} from "react";import {useRouter} from "next/navigation";
export function UserMenu({nombre}:{nombre:string}){const router=useRouter(),ref=useRef<HTMLDivElement>(null);const [open,setOpen]=useState(false),[busy,setBusy]=useState(false),[dark,setDark]=useState(()=>typeof window!=="undefined"&&localStorage.getItem("los-onis-theme")==="dark");useEffect(()=>{const close=(e:PointerEvent)=>{if(!ref.current?.contains(e.target as Node))setOpen(false)};document.addEventListener("pointerdown",close);return()=>document.removeEventListener("pointerdown",close)},[]);function toggleTheme(){const next=dark?"light":"dark";document.documentElement.setAttribute("data-theme",next);localStorage.setItem("los-onis-theme",next);setDark(!dark)}async function logout(){setBusy(true);const r=await fetch("/api/logout",{method:"POST"});if(r.ok){router.replace("/login");router.refresh()}else setBusy(false)}return <div className="user-menu" ref={ref}><button aria-label="Abrir menú de usuario" aria-expanded={open} onClick={()=>setOpen(!open)}><span className="material-symbols-outlined">menu</span></button>{open&&<div className="user-dropdown"><span>Sesión iniciada como</span><strong>{nombre}</strong><button className="theme-toggle" onClick={toggleTheme}><span className="material-symbols-outlined">{dark?"light_mode":"dark_mode"}</span>{dark?"Modo claro":"Modo oscuro"}</button><button disabled={busy} onClick={logout}><span className="material-symbols-outlined">logout</span>{busy?"Cerrando…":"Cerrar sesión"}</button></div>}</div>}
